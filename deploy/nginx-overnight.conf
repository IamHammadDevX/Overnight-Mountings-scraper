server {
    listen 80;
    server_name _;

    location /overnight/ {
        alias /opt/overnight-scraper/downloads/;
        autoindex off;
        add_header Content-Disposition "attachment";
        try_files $uri =404;
    }
}
